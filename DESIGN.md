# 声音世界交互规范（当前版本）

进入小宇宙后使用固定视口场景，底部四个坐标按钮切换声音原野、共鸣海岸、好奇星系、下载入口。手机内容过长时只在当前场景内滚动。产品内容仍是小宇宙介绍，不加入数字藏品交易功能。

## 显影实现

world.js 运行时生成 256×144、纵向排列的 24 帧透明 PNG Sprite Sheet。以五个圆的 signed distance 并集构造扩散边界，叠加连续正弦噪声，通过 smoothstep 映射为 alpha；首帧完全透明，末帧完全不透明。CSS mask-size 为 100% 2400%，requestAnimationFrame 在 1250ms 内推进帧索引，旧场景在新场景下面保留到揭示完成。首次进入同样使用图集显影。遮罩分辨率较低形成柔软边界，内容本身保留原始分辨率。

## 统一动效

交互缓动 cubic-bezier(.22,1,.36,1)。按钮悬停上移 4px、420ms；类别信息卡片使用 420ms 透视旋转展开；场景标题在 1200ms 内由 10px 模糊、20px 位移归位。背景 18s 缓慢推进、评论 7s 悬浮、星系轨道 70s 一周。图片解码期间轮换品牌短句，加载完成显示声音已就绪，不显示模拟进度。

## 操作与降级

底部按钮支持点击、Tab、左右方向键、Home/End；非当前场景 inert，切换结束聚焦标题；减少动态模式直接切换，无遮罩动画。页面隐藏时暂停 CSS 动画。保留音频主动播放与原有社区、类别、官方下载交互。

## 验证

语法检查、入口生命周期检查、24 帧 alpha 总量单调递增及首尾透明度断言通过。尚未完成真实浏览器视觉与性能验收。

## 粒子气泡交互更新

主导航改为悬浮能量球，场景空白点击同样前进，底部保留无文字定位点。排除链接、按钮、播放器、评论、类别详情和文字选区；拖动超过 8px 不触发。切换期间锁定重复请求。

点击位置作为转场原点，1250ms 内完成扩散圆环、粒子径向拉伸、旧场景 1.16 倍推进与 7px 模糊、新场景 0.94→1 缩放；继续使用原有 SDF 图集揭示。180 个粒子气泡，低核心设备 85 个，像素比限制 1.5/1。后台暂停、减少动态时取消粒子和推进，保留可键盘操作的能量球。
