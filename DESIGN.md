# 当前交互规范：裂开翻转

保留首页粒子与冰蓝、鸢尾紫配色。后续只使用双片裂开翻转，移除粒子爆炸、墨迹遮罩、光环和模糊推进。

切换前等待目标图片 decode；旧世界保持可见。以点击横坐标生成锯齿裂缝，复制旧场景为左右两片并锁定动画当前样式。下一世界完整放在底层，旧场景两片分别围绕屏幕左右边缘旋转 ±105°，950ms，cubic-bezier(.65,0,.2,1)，1500px 透视，不透明度不做淡黑处理。结束删除副本、恢复交互、聚焦标题。

桌面气泡 58px、跟随指针，系统箭头隐藏。悬停控件缩至 32px，pointer-events:none 保留原控件行为。离开视口或 Tab 导航恢复默认鼠标与可聚焦气泡按钮。触屏保留 64px 按钮和空白轻触前进。拖动超过 8px、选中文本、点击播放器或评论等不切换。

减少动态模式直接切换。通过 Node 逻辑检查验证底层下一场景持续存在、双片创建、重复切换锁定和最终清理。尚未完成真实浏览器视觉验收。
